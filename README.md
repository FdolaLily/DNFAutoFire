# DAF - 一款DNF连发工具

## chenyu 魔改版

本仓库当前成品为基于 DAF 的个人魔改版。

**2026-09-25 v0.2.0.0：** 启动直接驻留托盘、重复启动提示、7+7ms / 150ms 新默认值、连招与托盘菜单改进，版本号自此从 0.2 起。

**2026-09-24 v0.1.6.0 界面重做：** 仿机械键盘的全新界面，修改自动保存即时生效，可切换明亮/暗黑主题，内置界面字体。

**2026-09-24 原生客户端迁移：** v0.1.5.0 已将界面、配置和全部按键控制迁移为 C++ / Win32 单 EXE，移除 AHK 打包和临时 DLL 加载。旧版火绒检出记录及本次检测边界见 [安全复核记录](docs/security/HUORONG-20260924.md)。不应仅凭换架构或源码审查判定安全软件误报。

- 魔改版作者与提交者：[chenyu](mailto:fdola@qq.com)
- 联系邮箱：[fdola@qq.com](mailto:fdola@qq.com)
- 保留原项目作者与历史更新记录；v0.1.6.0 起换用与新界面一致的键帽图标，原图标设计者见下表。

魔改版主要变更：

- v0.1.4.0 将普通连发和职业连发统一到 C++ 原生引擎：事件驱动、QPC 绝对调度、高精度等待、失焦清理，取消每键子进程。
- 每方案可设置按下/抬起各 1–100ms，新方案默认 7+7ms；客户端仍为无需额外安装环境的单个 Windows x64 EXE。
- [架构审查、构建与验证边界](native/README.md)；[原生调度基准](native/BENCHMARK.md)。基准频率不等于 DNF 的技能释放频率。

- 移除“原地平X”功能。
- 增加停止、切换配置和退出时的按键补释放，降低卡键风险。
- 一键奔跑提供独立设置页；默认方向键对所有连发方案生效，也可为当前方案启用独立方向键覆盖。功能默认关闭，按下/双击延迟和游戏内开关热键可配置。
- 弹出设置页中的单键绑定统一改为点击输入框后直接按键，并支持 Delete、方向键和数字小键盘等特殊按键。
- 一键奔跑改用事件令牌确认输入意图：初次按下立即行走，默认持续 140ms 后只补一次抬起/按下，约 170ms 即可进入奔跑；350ms 内的不同方向序列优先按搓招处理，短促的技能输入也会永久取消本次待触发，避免轮询漏键。
- 奔跑确认阶段的抬起/再次按下采用原子提交，斜向第二方向会在首方向恢复按下后再处理，避免恰好卡在双击间隔时丢失一个移动轴。
- 最终奔跑按下提交后立即建立连续移动会话，不再额外等待 350ms；仍按住旧方向时新增正交方向会直接继承奔跑且不补双击，相反方向或短暂全松后的重新起步使用 90ms 异步确认，快速乱动不会再把方向热键压在等待线程后面。
- 技能释放后的奔跑恢复由主进程执行，只处理已经确认的奔跑会话；斜向会话选择最近方向重触发一次，不再把搓招中仍按住的方向误当成奔跑。
- 最终连发模式使用 10ms 按下 + 10ms 抬起，单键用于探索单技能释放极限。
- 同时按下两个或三个连发键时，按当前配置顺序使用固定 8ms 协同相位；四键以上回退动态错峰，不绑定具体键位。
- 战法炫纹发射键限定为数字小键盘按键，并提示使用时先按住发射键，再按 Num Lock 关闭数字小键盘。

## 自动构建与 GitHub Releases

[下载最新版本](https://github.com/FdolaLily/DNFAutoFire/releases/latest) · [查看构建记录](https://github.com/FdolaLily/DNFAutoFire/actions/workflows/build-release.yml)

- 推送 `main`、推送 `v*` 标签或在 Actions 中手动运行 `Build and Release`，都会在 Windows 上自动编译并运行全部回归；提交到 `main` 的 Pull Request 也会验证构建，但不发布。
- `main` 或版本标签构建通过后，读取根目录 `Version` 的 `tag_name` 和 `body`，为尚未发布的版本自动创建 GitHub Release。普通提交不会覆盖同版本的已发布附件；发布下一版时请完成下方版本文件同步后再推送。
- 附件包含 `DNFAutoFire.exe`、Windows x64 ZIP 和 `SHA256SUMS.txt`。Actions 的构建产物保留 30 天；Release 附件长期保留。失败的构建不会发布，上传中断会保留草稿，可对同一提交重新运行。
- 发布前校验标签、EXE 与 manifest 版本一致，校验 Zig 下载包和编译器的固定 SHA256，并执行已知检出样本黑名单检查。自动检查不等于杀毒软件主动扫描通过。
- 升级版本时同步 `Version`、`native/client.rc`、`native/client.manifest`、`native/client_ui.cpp`、`scripts/build.ps1`、`scripts/publish.ps1` 与本文件更新日志。推送后无需另外手动创建标签或 Release。
- 云端工作流使用 GitHub 托管 runner 与内置 `GITHUB_TOKEN`，无需配置个人令牌。本机 `E:\autokill`、`AutoManagerProcess` 的部署继续使用 `scripts/publish.ps1` 完成。

## 功能简介
 - 多键位无冲突连发
 - 不影响打字
 - 仅DNF游戏窗口内激活
 - 自动使用管理员模式启动
 - 效能媲美Wegame内置连发(基于个人测试)

## 原项目贡献人员
|                                                                             |                                                                              |
| --------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| <img src="https://avatars.githubusercontent.com/u/7844572?v=4" width="128"> | <img src="https://avatars.githubusercontent.com/u/28993260?v=4" width="128"> |
| [某亚瑟](https://github.com/mouyase)                                        | [Ousumu](https://github.com/1208041822)                                      |
| 所有者                                                                      | 原图标设计（v0.1.5.1 及以前）                                                |

## 更新日志

### 2026-9-25 v0.2.0.0

- [启动]设置项更名为“打开后直接开始连发，窗口隐藏到托盘”并默认开启：主窗口不再先显示再隐藏，直接驻留托盘；首次运行（尚无 config.ini）或自动启动失败时显示主界面。
- [交互]重复打开程序不再静默退出，而是唤出已运行实例的主界面，并在底栏提示“已在运行，无需重复打开”。
- [默认]新方案连发默认改为 7ms 按下 + 7ms 抬起，一键奔跑搓招保护默认 150ms；已有方案保留原值。低于 7ms 或 150ms 时数值变为橙色并在底栏说明漏键 / 误触奔跑风险。
- [交互]顶栏“游戏内切换”热键键帽可直接点击修改，与设置页共用校验。
- [连招]设置触发键时自动添加为第 1 个输出键（触发键变更时同步更新），新增输出键默认间隔 30ms；每个输出键右侧增加移除按钮。
- [托盘]托盘右键菜单改为自绘，沿用界面配色并随明亮 / 暗黑主题切换；显示运行状态与当前方案，启动 / 停止合并为一个开关。

### 2026-9-24 v0.1.6.0

- [界面]全新界面：仿高端机械键盘的全尺寸键位，键帽右上角绿灯表示已开启连发，运行时绿灯呼吸闪烁；键帽底部色条标出被一键奔跑（蓝）、一键连招（橙）、剑宗帝剑（紫）占用的键，冲突时亮红灯。
- [交互]取消“保存全部”，所有修改自动保存并即时生效，运行中无需重新启动；启动/停止合并为一个开关；新建、复制、重命名、删除方案收进方案标签的“⋯”菜单，双击方案名可直接重命名。
- [交互]连发时序、职业辅助、一键连招、一键奔跑以功能卡片呈现，细项在右侧面板调整；底栏提示随鼠标所指的键实时说明，取代使用说明页。
- [界面]明亮 / 暗黑主题一键切换并记住选择；游戏内快速切换窗口同步改版。
- [字体]内置 Noto Sans SC 与 JetBrains Mono（SIL OFL 1.1），仅在本进程内注册，不安装到系统。
- [图标]程序与托盘图标按新界面重绘：深色键帽、方波字符、右上角绿灯；托盘绿灯亮为运行中，灯灭为未启动。
- [工程]移除旧 AHK 实现、Python 原型、DLL 构建与历史构建产物，仓库只保留原生客户端源码、构建/发布脚本和相关文档。
- [架构]界面改为 Direct2D / DirectWrite 自绘，新增依赖均为 Windows 自带组件（d2d1、dwrite、dwmapi）；仍为单个 EXE，无需额外运行环境。

### 2026-9-24 v0.1.5.1

- [界面]普通连发按键恢复原版仿键盘布局，包含字母错位行、加长键、编辑导航区、方向键与数字小键盘；红色启用、蓝色关闭。
- [启动]明确链接为 Windows GUI 子系统，消除启动黑色控制台窗口；构建检查和成品自检共同防止回归。

### 2026-9-24 v0.1.5.0

- [架构]纯 C++ / Win32 单 EXE，界面、配置、奔跑和连招与原生调度引擎静态链接；不再打包 AHK 或释放临时 DLL。
- [兼容]继续使用原 config.ini，保留方案、别名、职业、连招和奔跑参数；固定 Global 互斥锁保持跨目录、改名和版本之间唯一。
- [验证]覆盖原配置读写、虚拟时钟状态机、调度、隐藏界面和最终成品零输入启动；支持 Windows 10/11 x64，无需额外安装运行环境。

### 2026-9-24 v0.1.4.2（未发布）

- [修复]加固内置原生 DLL 的文件加载过程：受保护私有目录、逐字节完整性验证、持有防写入/删除/改名句柄、限定系统依赖搜索。
- [兼容]保留单 EXE 和免安装运行环境；不改变连发调度、配置和跨目录单实例行为。
- [边界]这是针对实际加载风险的修复，不等于火绒已确认误报或完成扫描放行；以最终成品的实测记录为准。
- [结果]加固版仍在 Ahk2Exe 编译阶段被同一规则删除，未发布；随后移除该打包和装载架构。

### 2026-9-24 v0.1.4.1

- [修复]用固定 Windows 全局互斥锁代替依赖路径的 AHK 单实例判断；不同目录、改名副本与后续版本共用一个客户端实例。
- [启动]后启动的副本自动退出，先启动的实例继续运行；提权前检查已有实例，退出或异常终止后由 Windows 自动回收锁。
- [验证]覆盖不同路径并发争抢、正常退出和异常终止后重新启动。`--self-test` 只做零输入引擎自检，不启动客户端。

### 2026-9-24 v0.1.4.0

- [架构]普通连发、旅人、战法和剑宗触发统一到原生多键调度，配置在启动时缓存，同键只有一个周期输出来源。
- [稳定]绝对截止点消除累计休眠漂移；迟到跳周期，停止和失焦释放；连招通过按键交接避免同键发送竞争。
- [设置]增加每方案按下/抬起时序，1+1ms 为理论 500Hz 实验参数，默认继续使用 10+10ms。
- [部署]内置静态链接的 C++ 核心，Windows 10/11 x64 客户端不需要另装运行环境。
- [验证]通过调度与生命周期自动回归；游戏内实际释放率及职业交互仍需固定条件 A/B 验收。

### 2026-9-24 v0.1.3.5

- [修复]按住小键盘技能键后切换 Num Lock，数字键与导航键的松开事件按同一实体扫描码配对，避免残留键态持续阻止一键奔跑。
- [修复]切换预设、关闭奔跑期间持续监听物理松键；保留战法原有的 Num Lock 自动释放操作。
- [修复]输入监听不再积累文本，避免达到默认 1023 字符上限后停止监听。
- [验证]覆盖全部 11 组小键盘别名、方向键区分、开关期间松键、1100 次输入和连发注入过滤。

### 2026-9-5 v0.1.3.4

- [修复]数字小键盘等按键的物理状态缓存残留导致一键奔跑持续失效、重新开关仍无法恢复的问题。
- [优化]奔跑冲突检测同时核对 Windows 实时键态，忽略已经松开的陈旧按键状态。
- [验证]新增陈旧键态、真实按住、注入输入及斜向移动回归测试，保留原有搓招与奔跑触发时序。

### 2026-7-23 v0.1.3.3

- [特性]新增默认关闭的一键奔跑，可自定义四方向、触发延迟、双击间隔和游戏内开关热键；方向键默认对全部方案生效，也可为当前方案单独覆盖。
- [优化]奔跑采用输入令牌与连续移动会话：顺序搓招优先，斜向方向即时继承奔跑，相反方向异步快速切换。
- [修复]技能释放后仅恢复已确认的奔跑状态，并修复快速变向、多个方向同时按下时的停顿、丢轴和串键问题。
- [修复]一键连招严格限制在 DNF 游戏窗口范围内，不再影响游戏外输入。
- [界面]单键绑定统一改为直接输入，并支持 Delete、方向键和数字小键盘等特殊按键。

### 2026-7-12 v0.1.3.2

- [优化]正式版采用 8ms 协同相位：两键为 0/8ms，三键为 0/8/16ms。
- [兼容]四键以上继续使用原动态错峰，不写死具体键位。
- [界面]启动提示仅保留“连发已启动 - 当前方案”。

### 2026-7-12 v0.1.3.1 魔改最终版

- [特性]启用 10ms 高精度连发脉冲与多键动态错峰。
- [优化]增加按键统一补释放与后台轮询优化，降低卡键和卡顿风险。
- [调整]移除原地平X，增加战法数字小键盘限定与 Num Lock 操作提示。
- [作者/提交者]chenyu <fdola@qq.com>

### 2023-7-12 v0.1.2Fix3
 - [修复]修复小键盘部分功能失灵
 - [优化]优化启动连发后需要再点一下游戏窗口的问题

### 2023-7-12 v0.1.2Fix2
 - [修复]修复剑宗，战法，旅人功能失效
 - [优化]优化克隆配置时不能克隆功能详细的按键设置

### 2023-7-12 v0.1.2Fix1
 - [修复]修复通用设置内文字空白
 - [修复]修复无法使用游戏内自动捡物功能
 - [修复]修复太宗帝剑延迟失灵
 - [修复]修复某些情况下报错提示sc00 is not a valid key name的问题
 - [优化]优化了更新服务器，也许可以提高自动更新的成功率

### 2023-7-11 v0.1.2
 - [特性]增加了软件设置窗口
 - [特性]可以设置软件打开后自动启动连发
 - [特性]可以设置开机自动打开软件
 - [特性]可以设置是否游戏内屏蔽Windows键
 - [修复]修复了删除全部配置后会出现一个空配置的问题
 - [修复]修复了太宗帝剑延迟失灵的问题
 - [优化]优化了部分窗口隐藏逻辑
 - [优化]优化了更新服务器，也许可以提高自动更新的成功率
 - [优化]优化了部分配置相关代码

### 2023-7-7 v0.1.1
 - [特性]增加了全职业原地平X功能(测试版)
 - [修复]修复了快速切换窗口不能按回车点击按钮
 - [修复]修复了打开快速切换窗口热键设置的异常
 - [优化]优化按键输入逻辑
 - [优化]优化快速切换窗口，现在可以双击列表选项切换配置

### 2023-7-4 v0.1.0
 - [特性]增加了切换配置的提示
 - [特性]增加了连发状态的托盘图标样式
 - [特性]增加了自定义快速切换窗口快捷键的功能
 - [特性]增加了可以在快速切换窗口中按空格切换配置
 - [修复]修复了Shift，Alt，Ctrl在某些情况下会卡住的情况
 - [修复]修复了旅人自动流星概率发射失败的问题
 - [优化]优化了逻辑判断，减少了CPU的占用
 - [优化]优化按键输入逻辑，减少了按键输入延迟
 - [优化]优化了部分窗口可以点X或者按Esc关闭

### 2023-6-30 v0.0.10
 - [修复]修复了配置保存时的逻辑问题
 - [优化]优化了按键触发逻辑
 - [特性]增加了战法自动炫纹功能
 - [特性]增加了太宗帝剑延迟功能

### 2023-6-30 v0.0.9
 - [修复]修复了切换配置导致按键失灵的问题
 - [优化]优化了配置切换逻辑
 - [优化]优化了自动更新流程，现在可以自动释放更新脚本
 - [特性]增加了旅人自动流星功能
 - [特性]增加了基本使用说明

### 2023-6-29 v0.0.8
 - 优化了连发效率
 - 增加了自动更新功能

### 2023-6-28 v0.0.7
 - 增加了在游戏中按下 Alt + ` 可以弹出快速切换窗口
 - 在快速切换窗口中可以按上下键选择预设，按回车切换预设并开启连发
 - 优化代码结构
 - 优化设置文件结构，旧设置会失效
 - 为连发开启与关闭添加声音提示
 - 去除托盘中错误的菜单选项

### 2023-6-27 v0.0.6
 - 修复Caps等按键原有功能失效
 - 修复Num0失灵
 - 修复部分功能按键失灵

### 2023-6-27 v0.0.5
 - 修复部分按键失灵的问题

### 2023-6-27 v0.0.4
 - 优化了按键触发逻辑，减少卡键问题
 - 优化了Shift，Ctrl和Alt的触发
 - 去掉了左上角连发启动的提示，改为托盘图标变更
 - 增加了连发启动的提示音
 - 更换了新的图标

### 2023-6-25 v0.0.3
 - 修复了X键不生效的错误
 - 优化了图标获取方式，压缩包里不再需要图标文件

### 2023-6-24 v0.0.2
 - 优化了实现逻辑，减少了CPU占用

### 2023-6-24 v0.0.1
 - 首次发布的版本
 - 实现了最基础的连发功能
